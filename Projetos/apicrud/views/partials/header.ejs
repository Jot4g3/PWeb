<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Library</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Libertinus+Math&display=swap" rel="stylesheet">
    <link rel="shortcut icon" type="image/x-icon" href="/favicon.svg">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    <style>
        .bg-custom-blue {
            background-color: #2b5594;
        }

        .card-reserved {
            background-color: rgba(48, 84, 148, 0.15) !important;
            /* Cor azul com opacidade */
            border: 1px solid rgba(48, 84, 148, 0.4);
        }

        .card-not-reserved {
            background-color: rgba(94, 103, 118, 0.07) !important;
            /* Cor azul com opacidade */
            border: 1px solid rgba(94, 103, 118, 0.2);
        }
    </style>

</head>

<body class="vh-100">
    <nav class="navbar navbar-expand-lg navbar-dark bg-custom-blue shadow-sm">
        <div class="container-fluid">
            <a class="navbar-brand fs-3 fw-bold" href="/">the library</a>

            <div class="d-flex align-items-center">

                <% if (session && session.userName) { %>
                    <div class="dropdown">
                        <button class="btn btn-outline-light dropdown-toggle d-flex align-items-center" type="button"
                            data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="bi bi-person-circle me-2"></i>
                            <%= session.userName %>
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li>
                                <h6 class="dropdown-header">
                                    <% if (session.isLibrarian) { %>
                                        <i class="bi bi-bookmark-star-fill me-1"></i> Bibliotecário
                                        <% } else { %>
                                            <i class="bi bi-person-fill me-1"></i> Leitor
                                            <% } %>
                                </h6>
                            </li>
                            <li><a class="dropdown-item" href="/profile">Meu Perfil</a></li>
                            <li>
                                <hr class="dropdown-divider">
                            </li>
                            <li><a class="dropdown-item text-danger" href="/logout">
                                    <i class="bi bi-box-arrow-right me-2"></i>Sair
                                </a></li>
                        </ul>
                    </div>

                    <% } else { %>
                        <div class="d-flex gap-2">
                            <a href="/login" class="btn btn-light">Entrar</a>
                            <a href="/register" class="btn btn-outline-light">Registrar-se</a>
                        </div>
                        <% } %>

            </div>
        </div>
    </nav>

    <div class="container mt-3">
        <% if (success_msg && success_msg.length> 0) { %>
            <div class="alert alert-success">
                <%= success_msg %>
            </div>
            <% } %>
                <% if (error_msg && error_msg.length> 0) { %>
                    <div class="alert alert-danger">
                        <%= error_msg %>
                    </div>
                    <% } %>
    </div>

    <div class="d-flex justify-content-center align-items-center">